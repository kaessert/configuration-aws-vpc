"""
Shared test utilities and helper functions.

This directory contains reusable code but is not itself a test.
The empty items array allows the test runner to process this directory without errors.
"""

# Export empty items array (required by test runner)
_items = []
items = _items
